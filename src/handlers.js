const notes = require('./notes');

const addNote = () => {
  return null;
};

const getANote = () => {
  return null;
};

const getNotes = () => {
  return notes;
};

const replaceNote = () => {
  return null;
};

const deleteNote = () => {
  return null;
};

module.exports = { addNote, getANote, getNotes, replaceNote, deleteNote };
